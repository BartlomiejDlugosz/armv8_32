int main(int argc, char **argv) {

    return 0;
}




/*
 change_function = &minimax;
while (true) { // timestep
    update_lights_to_next_state(change_function) // hmm, is it time to     update? minimax

    for (each road) {
        update_distances() // pop off ANY cars which have passed stop l    ine
        remove_crossed() // pop off ANY cars which have passed stop lin    e
        maybe_add_cars() // random function + also check sum < max_leng    th
    }
}

*/
