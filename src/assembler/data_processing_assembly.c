#include "data_processing_assembly.h"
#include "dynamicString.h"
#include <stdint.h>
#define MULTIPLY_ADD "madd"
void data_processing_init(char *opcode, dynamicString operands) {

    int operand_num = sizeof(operands)/sizeof(operands[0]);
    if()
    return;
}
char** operand_to_operand_list(char *operands) {
    char** result;
    
}
uint32_t multiply_instructions(char *mulopcode, char **operands) {
    if(strcmp(mulopcode, MULTIPLY_ADD) == 0) {

    }
}